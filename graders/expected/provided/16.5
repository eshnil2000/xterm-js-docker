int main(int argc, char ** argv) {
  size_t n = atoi(argv[1]);
  int max = atoi(argv[2]);
  int seed= atoi(argv[3]);
  srandom(seed);
  int * array = malloc(n * sizeof(*array));
  for (int i = 0; i < n; i++) {
    array[i] = random() % max;
  }
  doTest (array, n);
  free(array);
  return EXIT_SUCCESS;
}

